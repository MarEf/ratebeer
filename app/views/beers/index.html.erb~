<p id="notice"><%= notice %></p>

<h1>Listing Beers</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Style</th>
      <th>Brewery</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @beers.each do |beer| %>
			<% byebug if beer.brewery.nil? %>
      <tr>
        <td><%= link_to beer.name, beer %></td>
        <td><%= link_to Style.find(beer.style_id).name, style_path(beer.style_id) %></td>
        <td><%= link_to Brewery.find(beer.brewery_id).name, brewery_path(beer.brewery_id) %></td>
        <td><%= edit_and_destroy_buttons(@beer) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to('New Beer', new_beer_path, class:'btn btn-primary') if current_user %>
<%= link_to 'List of breweries', breweries_path, class:'btn btn-primary' %>
